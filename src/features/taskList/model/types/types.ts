export enum StatusesEnum {
    ONLINE = "Online",
    PAUSED = "Paused",
    STOPPED = "Stopped",
    DRAFT = "Draft"
}

export enum TestTypesEnum {
    MVT = "MVT",
    CLASSIC = "Classic",
    SERVER_SIDE = "Server-side"
}
